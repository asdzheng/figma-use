# TODO

## Commands to Add

- [x] `node bounds <id>` — get bounding box (x, y, width, height, center)
- [x] `path get <id>` — get vectorPaths from vector node
- [x] `path set <id> "..."` — update vectorPaths on vector node
- [x] `path move <id> --dx --dy` — move all path points
- [x] `path scale <id> --factor` — scale from center
- [x] `path flip <id> --axis x|y` — mirror path

## Future Ideas

- [ ] `path append <id> "..."` — add subpath to existing vector
- [ ] `path simplify <id>` — reduce number of points
- [ ] `path smooth <id>` — smooth corners
